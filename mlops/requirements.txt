# Pinned for reproducibility — all `==`, not `>=`.
# Tested on Python 3.10. Update REPRODUCING.md if you bump anything here.

# ── Core ML ──
torch==2.3.0
torchvision==0.18.0
transformers==4.40.2
segmentation-models-pytorch==0.3.4
huggingface-hub==0.23.0
accelerate==0.30.1

# ── Geospatial / SAR ──
rasterio==1.3.10
scikit-image==0.22.0

# ── Numerics ──
numpy==1.26.4
pandas==2.2.2
matplotlib==3.8.4

# ── MLOps ──
clearml==1.16.2
onnx==1.16.1
onnxruntime==1.18.0

# ── App layer ──
streamlit==1.34.0
fastapi==0.111.0
uvicorn==0.30.1
